const config = {
  prefix: "!",
  botToken: process.env.BOT_TOKEN,
  // welcomeText: "ยินดีต้อนรับ",
  // emblemText: "🌟 Welcome",
  welcomeBackground: "./assets/sp.png",
};

export default config;
